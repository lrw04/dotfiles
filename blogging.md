# Blogging

```
sudo apt install -y poppler
curl --proto '=https' --tlsv1.2 -fsSL https://drop-sh.fullyjustified.net | sh
git clone https://github.com/lrw04/bwog
cd bwog
ros build bwog.ros
mkdir -p ~/.local/bin
mv bwog ~/.local/bin
```
